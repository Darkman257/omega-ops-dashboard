kind = "web"
previewPath = "/"
title = "Omega Tech Dashboard"
version = "1.0.0"
id = "artifacts/omega-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20108

[services.development]
run = "pnpm --filter @workspace/omega-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/omega-dashboard", "run", "build" ]
publicDir = "artifacts/omega-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20108"
BASE_PATH = "/"
